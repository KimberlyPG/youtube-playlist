import { Outlet } from "react-router-dom";

const Authentication = () => {
    return (
        <div>
            <Outlet />
            Authentication page
        </div>
    )
}

export default Authentication;
